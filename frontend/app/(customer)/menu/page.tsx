// Menu page — category tabs + item cards
// Phase 1: static mock data  |  Phase 2: fetch from GET /api/menu/
export default function MenuPage() {
  return <div className="page-header"><h1>Menu</h1></div>;
}
