// Table & QR management — add tables, generate + download QR codes
// Phase 3: GET/POST /api/admin/tables/  |  QR encodes /?table={token}
export default function AdminTablesPage() {
  return <div><h1 className="font-display font-bold text-3xl">Tables & QR Codes</h1></div>;
}
