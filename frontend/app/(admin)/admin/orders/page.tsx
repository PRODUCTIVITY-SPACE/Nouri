// Live orders board — active order cards + status update buttons
// Phase 3: real-time via WebSocket  |  POST /api/admin/orders/{id}/status/
export default function AdminOrdersPage() {
  return <div><h1 className="font-display font-bold text-3xl">Orders</h1></div>;
}
