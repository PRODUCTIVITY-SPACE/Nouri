// Admin login — email + password form
// Phase 2: POST /api/auth/login/ → JWT stored in httpOnly cookie
export default function AdminLoginPage() {
  return <div className="min-h-screen flex items-center justify-center"><h1>Admin Login</h1></div>;
}
